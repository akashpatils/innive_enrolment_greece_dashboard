export const metadata = {
  title: "Population/Census",
  description: "Welcome to Greece Enrolment Dashboard",
  icons: {
    icon: [{ url: "/logo.svg" }],
  },
};

export default function PopulationCensusLayout({ children }) {
  return (

    <>
      {children}
    </>
  );
}
